# Lower bounds (not exact pins) so pip pulls wheels with Python 3.14 support.
# The old google-cloud-datastore==2.15.2 dragged in a protobuf whose C extension
# crashes on import under Python 3.14 ("Metaclasses with custom tp_new are not
# supported"), which is what caused the 502 / "Worker failed to boot".
Flask>=3.1
google-cloud-datastore>=2.21
protobuf>=5.29
gunicorn>=23.0
