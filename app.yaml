# App Engine Standard (Python 3). No `entrypoint`, so GAE runs `gunicorn main:app`
# automatically — the Flask object must be named `app` in main.py.
runtime: python314

handlers:
# Route everything to the Flask app.
- url: /.*
  script: auto
